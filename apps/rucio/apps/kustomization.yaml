apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - cvmfs-test-pod.yaml
  - cacj.yaml

generatorOptions:
  disableNameSuffixHash: true

secretGenerator:
- name: dir-secret
  namespace: cvmfs-03
  files:
  - Customizations
